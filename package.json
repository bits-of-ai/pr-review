{
  "name": "pr-review",
  "version": "1.1.0",
  "private": true,
  "description": "AI reviewer for GitHub pull requests, hosted on Cloudflare Pages.",
  "type": "module",
  "scripts": {
    "start": "npx wrangler dev --port 4173",
    "test": "node --test tests/*.test.js",
    "pages:deploy": "npx wrangler deploy"
  },
  "engines": {
    "node": ">=20"
  }
}