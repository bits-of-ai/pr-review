{
  "name": "pr-review",
  "version": "1.1.0",
  "private": true,
  "description": "AI reviewer for GitHub pull requests, hosted on Cloudflare Workers.",
  "type": "module",
  "scripts": {
    "start": "wrangler dev --port 4173 --ip 127.0.0.1",
    "test": "node --test tests/*.test.js",
    "deploy": "wrangler deploy"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
